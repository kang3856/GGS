<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="responses">
	<insert id="insertE" parameterType="Response">
		INSERT INTO responses(no, rank, respondent_no, category_no, regdate)
		VALUES(responses_seq.nextval, #{rank}, #{respondentNo}, #{categoryNo}, systimestamp)
	</insert>
</mapper>  




